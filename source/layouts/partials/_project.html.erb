<% data.projects.details.each do |info| %>
  <section class="project-wrapper">
    <div class="project-container">
      <p class="title"><%= info.title %></p>
      <p class="plus"> + </p>
    </div>
  </section>
<% end %>
